'use strict';

angular.module('app.controllers')
.controller('SessionCtrl', function($scope, $modal, $routeParams, _) {

  /*
  $http.get('/api/sessions/'+$routeParams.sessionID)
    .success(function(session, length) {
      console.log('Session', session, length);
    }
  );
*/
/*
  Questions.sync().then(function() {
    $scope.data = Questions.countries($scope.currentPage);
  });
*/
});
